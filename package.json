{
  "name": "publint-monorepo",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "lint": "prettier \"**/*.{js,ts,css,md,vue,svelte,html}\" --check",
    "format": "prettier \"**/*.{js,ts,css,md,vue,svelte,html}\" --write",
    "lint-publint": "pnpm --filter \"./packages/*\" --no-bail run lint-publint",
    "typecheck": "pnpm --parallel --no-bail run typecheck",
    "test": "pnpm --filter \"./packages/*\" --filter \"./site\" run test"
  },
  "packageManager": "pnpm@10.17.0",
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@changesets/cli": "^3.0.0-next.0",
    "@svitejs/changesets-changelog-github-compact": "^1.2.0",
    "@types/node": "^24.9.1",
    "@types/pnpapi": "^0.0.5",
    "prettier": "^3.6.2",
    "prettier-plugin-svelte": "^3.4.0",
    "svelte": "^5.42.2",
    "typescript": "~5.9.3"
  },
  "pnpm": {
    "overrides": {
      "prettier": "$prettier"
    },
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp"
    ]
  }
}